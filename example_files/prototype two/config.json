{
  "_doc": {
    "purpose": "Consolidated config for prototype two (paths, data, model, training, inference, viewer)",
    "usage": [
      "Infer: python infer.py --config config.json",
      "Viewer: python heatmap_viewer.py --config heatmap_viewer_config.json or point to this file"
    ]
  },

  "paths": {
    "outputs_root": "data/outputs"
  },
  "seed": 42,
  "device": "auto",
  "model_type": "cnn",

  "data": {
    "train_field_path": "data/training data/laminar sparse_flow_vectors.csv",
    "test_field_path": "data/testing data/turbulent sparse_flow_vectors (2).csv",
    "tile_size": [256, 256],
    "tile_stride": [256, 256],
    "add_magnitude": true,
    "normalize": true,
    "limit_tiles": 64
  },

  "model": {
    "num_classes": 2,
    "conv_channels": [32, 64, 128, 256],
    "kernel_size": 3,
    "pool_every": 1,
    "dropout": 0.1,
    "use_batchnorm": true,
    "dilations": [1, 2, 4, 4]
  },

  "training": {
    "batch_size": 16,
    "num_workers": 0,
    "pin_memory": false,
    "val_split": 0.2,
    "learning_rate": 0.001,
    "weight_decay": 0.0,
    "optimizer": "adamw",
    "scheduler": "none",
    "max_epochs": 50
  },

  "inference": {
    "use_test": true,
    "generate_heatmap": true,
    "generate_confusion_matrix": true,
    "show_heatmap": false,
    "heatmap_class": 0,
    "cm_csv": "data/outputs/confusion_matrix.csv",
    "cm_png": "data/outputs/confusion_matrix.png",
    "heatmaps_dir": "data/outputs/heatmaps",
    "stitched_heatmap": "data/outputs/stitched_heatmap.png",
    "labels_npy": "",
    "class_names": ["class0", "class1"]
  },

  "viewer": {
    "viewer_values_csv": "data/testing data/heatmap_values_example.csv",
    "viewer_title": "Stitched Heatmap (Example)",
    "viewer_cmap": "viridis",
    "viewer_no_show": false,
    "viewer_save": "data/outputs/viewer_heatmap.png",
    "viewer_rows": 4,
    "viewer_cols": 4
  }
}
