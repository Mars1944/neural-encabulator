# Neural Encabulator - Prototype Two
# Target runtime: Python 3.12
#
# PyTorch packages are pinned as a compatible set for Python 3.12.
# If you want CUDA builds, install the same versions from the matching
# PyTorch index URL instead of the default CPU wheels.
#
# Flowchart generation also requires Graphviz system binaries with `dot`
# available on PATH; the `graphviz` package below is only the Python wrapper.

torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0
numpy==2.4.2
opencv-python==4.12.0.88
Pillow==12.1.1
matplotlib==3.10.8
tqdm==4.67.3
psutil==7.2.2
scipy==1.17.0
graphviz==0.21
